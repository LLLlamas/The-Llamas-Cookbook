# State Archive

Archived snapshot from the earlier SwiftUI port. It is no longer the current source of truth.

Use:

- `CLAUDE.md` for current agent instructions.
- `PROJECT.md` for durable product/workflow context.
- Code for exact behavior.

## Still Useful Notes

- Local recipe data is SwiftData and intentionally not CloudKit-synced.
- `RecipeStep.image` is deprecated; current photos use relationship models.
- Shared helpers exist for quantity formatting, pluralization, image processing, haptics, and keyboard handling.
- The archived RN implementation remains under `outdated/rn-expo/` for historical reference only.

## Current Gaps

See `ROADMAP.md`.
