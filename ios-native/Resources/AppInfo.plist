<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>$(DEVELOPMENT_LANGUAGE)</string>
    <key>CFBundleDisplayName</key>
    <string>Llamas Cookbook</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
    </array>
    <key>UIStatusBarStyle</key>
    <string>UIStatusBarStyleDefault</string>
    <key>NSSupportsLiveActivities</key>
    <true/>
    <!--
      Silent-push wake-up for slice 6's CloudKit subscriptions.
      Without `remote-notification` in UIBackgroundModes, iOS
      delivers silent pushes (`content-available: 1`) only when
      the app is foregrounded; suspended-in-background apps
      never get the wake-up call, defeating the "real-time
      presence beyond the foreground refresh" goal in
      implement-social.md slice 6.

      Enabling this background mode is a static-declaration-
      only step (no code path beyond the existing
      `application(_:didReceiveRemoteNotification:fetchCompletionHandler:)`
      handler in AppDelegate). Apple App Review checks that
      apps declaring `remote-notification` actually use it for
      content updates; we do (FriendsStore refresh + recipe
      import-count refresh on push receipt), so the declaration
      is honest.
    -->
    <key>UIBackgroundModes</key>
    <array>
        <string>remote-notification</string>
    </array>
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
    <key>UIDesignRequiresCompatibility</key>
    <true/>
    <!--
      Camera permission, required by the photo-import path: the
      "Import From Photo" entry in the Library FAB presents
      `UIImagePickerController` (sourceType = .camera) which uses the
      camera. App Reviewer auto-rejects builds that present a camera
      UI without a usage-description string. The string itself doubles
      as the user-facing copy in the iOS permission prompt — keep it
      honest about scope ("photos stay on your device") so users don't
      worry we're shipping their kitchen-counter photos somewhere.
    -->
    <key>NSCameraUsageDescription</key>
    <string>Llamas Cookbook uses the camera to scan recipe pages. The photos stay on your device.</string>
    <key>CFBundleURLTypes</key>
    <array>
        <dict>
            <key>CFBundleURLName</key>
            <string>com.llamascookbook.app</string>
            <key>CFBundleURLSchemes</key>
            <array>
                <string>llamascookbook</string>
            </array>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
        </dict>
    </array>
    <!--
      Recipe-share file format (.llamarecipe). The exported UTType +
      CFBundleDocumentTypes pair tells iOS we own the file extension,
      so AirDrop / Files / Mail / Messages route incoming
      `.llamarecipe` attachments straight into the app via onOpenURL.
      Identifier MUST match `UTType.llamaRecipe` in
      Sources/Lib/RecipeShare.swift — typo here means the type fails
      to register and AirDrop falls back to "Unknown app". See
      Recipe-Sharing.md §9.
    -->
    <key>UTExportedTypeDeclarations</key>
    <array>
        <dict>
            <key>UTTypeIdentifier</key>
            <string>com.llamascookbook.recipe</string>
            <key>UTTypeDescription</key>
            <string>Llamas Cookbook Recipe</string>
            <key>UTTypeConformsTo</key>
            <array>
                <string>public.json</string>
                <string>public.data</string>
                <string>public.content</string>
            </array>
            <key>UTTypeTagSpecification</key>
            <dict>
                <key>public.filename-extension</key>
                <array>
                    <string>llamarecipe</string>
                </array>
                <key>public.mime-type</key>
                <array>
                    <string>application/x-llamas-recipe+json</string>
                </array>
            </dict>
        </dict>
    </array>
    <key>CFBundleDocumentTypes</key>
    <array>
        <dict>
            <key>CFBundleTypeName</key>
            <string>Llamas Cookbook Recipe</string>
            <key>LSItemContentTypes</key>
            <array>
                <string>com.llamascookbook.recipe</string>
            </array>
            <key>LSHandlerRank</key>
            <string>Owner</string>
            <key>CFBundleTypeRole</key>
            <string>Editor</string>
        </dict>
    </array>
    <!--
      Required when CFBundleDocumentTypes is declared — App Store
      Connect emits ITMS-90737 otherwise. Set to NO because we don't
      edit the source file: importing a `.llamarecipe` materializes
      a fresh Recipe into SwiftData and the source bytes are no longer
      needed. iOS copies the file into our Inbox before handing the
      URL to onOpenURL, which is the right semantic for a one-shot
      importer (vs. document-based apps that hold security-scoped
      access to keep editing the original).
    -->
    <key>LSSupportsOpeningDocumentsInPlace</key>
    <false/>
</dict>
</plist>
