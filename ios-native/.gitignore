# XcodeGen generates this from project.yml. Do not commit it — regenerated on
# every `xcodegen generate` and on every CI run.
*.xcodeproj
*.xcworkspace

# Xcode build output
build/
DerivedData/
.build/

# Xcode user state
xcuserdata/
*.xcuserstate

# Swift Package Manager
Packages/
Package.resolved

# CocoaPods (unused for now but just in case)
Pods/

# Misc
.DS_Store
